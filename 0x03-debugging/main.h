#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

int largest_number(int a, int b, int c); // Function prototype for finding the largest number
void print_remaining_days(int month, int day, int year); // Function prototype for printing the remaining days in a year
int convert_day(int month, int day); // Function prototype for converting a date to the day of the year
void positive_or_negative(int i); // Function prototype for determining if a number is positive or negative
#endif
